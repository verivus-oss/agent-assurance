# NEGATIVE (defect: RKV02). The request inlines a secret-bearing `authorization`
# header as a raw field instead of pinning it via the canonical request
# descriptor digest. validate_api_snapshot.py MUST reject it.
#
# The value is a visible non-secret placeholder (NOT a real credential), so this
# fixture itself contains no inlined secret — it is the PRESENCE of the
# `authorization` field that RKV02 forbids. No [provenance] table, so the
# closure_root is the empty sentinel and the provenance sweep skips this file.
closure_root = "sha256:03e3be2c12c77646183d3f2d8493ed5f08e1089b24be976b80148d990736f44c"

[meta]
schema_version = "0.1.0"
template_kind = "api-snapshot"
framework_profile = "com.verivus.runtime"
confidentiality = "restricted"

[snapshot]
captured_at = "2026-06-17T09:14:02.481Z"
source_id = "https://api.exchange.example/v1/quotes/USD-AUD"

[snapshot.request]
method = "GET"
url = "https://api.exchange.example/v1/quotes/USD-AUD?asof=2026-06-17T09:14:00Z&pair=USD-AUD"
significant_headers = ["accept"]
descriptor_sha256 = "sha256:1e84dff27040181e39a0eedd71497241ae153efb65ec93e4b97303a8cb041869"
authorization = "<redacted-example-not-a-real-credential>"

[snapshot.response]
status = 200
body_sha256 = "sha256:569cc4222c936a5c39184bc0e17048c3f80bed44d457f0327db1bd58faecfebd"

# Asserted-unwitnessed so this fixture keeps isolating its headline RKV02
# defect rather than also tripping the RKV03 required-witness rule.
[snapshot.witness]
present = false
