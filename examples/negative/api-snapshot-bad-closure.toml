# NEGATIVE FIXTURE: MUST be rejected by the SPEC 12.8/12.8.1 closure gate
# in all three implementations. Since the profile pinned the api-snapshot
# component and witness digests as closure records (SPEC 12.8.1), the
# correct root for this witnessed document is the FOUR-record stream; the
# value below is the pre-promotion source-only root, now stale. (This
# fixture's polarity inverted at the promotion: the four-input root it
# used to carry as an illustration became the correct value.) It also
# keeps the deliberately-wrong provenance byte count so the provenance
# binding rejects it independently. Excluded from the positive closure
# sweep via --exclude; asserted to fail in the negative-agreement step.

#
# Secondary: because this fixture lives in examples/negative/ and declares a
# [provenance] table, CI's provenance-negative sweep also requires it to fail
# validate_provenance.py — so source_bytes is deliberately wrong (621, the
# capture is 620 bytes). The provenance digest is left correct so the closure
# validator still computes the source-only expected from a real source hash.
closure_root = "sha256:f251f64bc6170cb32a4b3c0bcc10d520247c41e7bbf22587d206108e6d19098c"

[meta]
schema_version = "0.1.0"
template_kind = "api-snapshot"
framework_profile = "com.verivus.runtime"
confidentiality = "restricted"

[provenance]
source_path = "examples/captures/2026-06-17T091402Z-usd-aud.capture"
source_sha256 = "sha256:1af17db5085faf2b3078538922f462ed9a37ee3b8e0d0b11d5aa34276442eab7"
source_bytes = 621

[snapshot]
captured_at = "2026-06-17T09:14:02.481Z"
source_id = "https://api.exchange.example/v1/quotes/USD-AUD"

[snapshot.request]
method = "GET"
url = "https://api.exchange.example/v1/quotes/USD-AUD?asof=2026-06-17T09:14:00Z&pair=USD-AUD"
significant_headers = ["accept"]
descriptor_sha256 = "sha256:1e84dff27040181e39a0eedd71497241ae153efb65ec93e4b97303a8cb041869"

[snapshot.response]
status = 200
body_sha256 = "sha256:569cc4222c936a5c39184bc0e17048c3f80bed44d457f0327db1bd58faecfebd"

[snapshot.witness]
present = true
scheme = "tls-notary"
observed = "both"
attester_id = "notary.example/ed25519:9f86d0818411"
attestation_sha256 = "sha256:afdb2e991b18d3b37506f893a987ba6fdc8888f7b474835bb81aeb2941e26d14"
