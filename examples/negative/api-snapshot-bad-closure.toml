# NEGATIVE (headline defect: closure_root).
#
# This file carries the profile-PROPOSED four-input closure value
# (sha256:013f3d34…), folded over source + descriptor + body + attestation.
# The shipped validate_closure_root.py (SPEC 12.8) folds ONLY
# provenance.source_sha256, so the expected value here is the source-only
# closure sha256:f251f64b… (the same value the positive instance carries).
# The four-input value therefore MUST be rejected by the closure validator in
# all three implementations until the richer record forms are promoted into
# SPEC 12.8.
#
# template_kind is the UNBLESSED `api-snapshot-bad-closure` (not the spec-reserved
# `api-snapshot`), so this deliberately-non-conforming document is out of the §12
# closure discovery scope per spec.md §12.1 (process-artifact TOMLs with unblessed
# template_kind values are intentionally out of scope). The closure rule is
# cross-kind and kind-agnostic, so the negative-agreement gate still rejects this
# file in all three implementations via explicit `--mode provenance` /
# validate_closure_root.py — the value below is rejected because the source-only
# §12.8 fold expects sha256:f251f64b...
#
# Secondary: because this fixture lives in examples/negative/ and declares a
# [provenance] table, CI's provenance-negative sweep also requires it to fail
# validate_provenance.py — so source_bytes is deliberately wrong (621, the
# capture is 620 bytes). The provenance digest is left correct so the closure
# validator still computes the source-only expected from a real source hash.
closure_root = "sha256:013f3d34bab26a1b9d9fd77ff03aae76a3b07ee112c4995dc5ef448b2d1796db"

[meta]
schema_version = "0.1.0"
template_kind = "api-snapshot-bad-closure"
framework_profile = "com.verivus.runtime"
confidentiality = "restricted"

[provenance]
source_path = "examples/captures/2026-06-17T091402Z-usd-aud.capture"
source_sha256 = "sha256:1af17db5085faf2b3078538922f462ed9a37ee3b8e0d0b11d5aa34276442eab7"
source_bytes = 621

[snapshot]
captured_at = "2026-06-17T09:14:02.481Z"
source_id = "https://api.exchange.example/v1/quotes/USD-AUD"

[snapshot.request]
method = "GET"
url = "https://api.exchange.example/v1/quotes/USD-AUD?asof=2026-06-17T09:14:00Z&pair=USD-AUD"
significant_headers = ["accept"]
descriptor_sha256 = "sha256:1e84dff27040181e39a0eedd71497241ae153efb65ec93e4b97303a8cb041869"

[snapshot.response]
status = 200
body_sha256 = "sha256:569cc4222c936a5c39184bc0e17048c3f80bed44d457f0327db1bd58faecfebd"

[snapshot.witness]
present = true
scheme = "tls-notary"
observed = "both"
attester_id = "notary.example/ed25519:9f86d0818411"
attestation_sha256 = "sha256:afdb2e991b18d3b37506f893a987ba6fdc8888f7b474835bb81aeb2941e26d14"
