### 1. SESSION META
Model name+version: Codex, GPT-5-class model; exact served model build not exposed in-session.
Sandbox/approval posture: workspace-write filesystem sandbox; writable roots include this repo and /tmp; approval policy never; shell network restricted. Web browsing was available separately.
MCP servers available: sqry MCP discovered and used (`get_index_status`: hasIndex=true, hasRelations=true, filesIndexed=44, indexedSymbols=7194); GitHub app MCP was discoverable but only PR metadata tooling was exposed.
Document opened: `paper/main.tex`.
Re-derived sha256: `9664be061e4bc8493de2e5054d3c3d61737bd666e56077b9856c19585dbde306`.
Repo commit observed: `638a90e8a0dc68c3e4aa8dfa29b51f6466d435fe`.
Dirty worktree note: the worktree was dirty before this review, including `paper/main.tex`; I did not revert or rely on unrelated changes.

Commands actually run and status:
`sha256sum paper/main.tex`: exit 0, hash matched above.
`python3 paper/figures/scripts/validate_numbers.py`: exit 0, printed `all agree: True`.
`bash examples/proof-chardet-relicense/detect.sh`: exit 0, but C06e emitted `SKIP`, quote: `behavioural fingerprint skipped: v6 install from worktree failed: ERROR: Failed to build 'file:///tmp/tmp.TYoAkTeA0L/v6' when installing build dependencies`.
`cd paper && make clean && make`: exit 2 because `pdflatex` is absent, quote: `make: pdflatex: No such file or directory`.
Bundle validators: implementation DAG, traceability, readiness gate, evidence matrix, and contract declaration all exited 0 with `... VALIDATION PASSED`.
JPlag: I inspected persisted JSON and runInfo; I did not rerun `run_jplag.sh` because `paper/figures/scripts/run_jplag.sh:26-28` fetches the jar with `curl`, shell network is restricted, and no local JPlag jar was present.
sqry: local CLI `sqry 16.0.6` was available; I re-indexed chardet v6/v7 worktrees and reproduced key table values: v6 `file_count=122`, `node_count=7947`, `edge_count=10915`; v7 `file_count=64`, `node_count=4292`, `edge_count=5803`; call cycles were 0/0.

### 2. PROCESS CONFIRMATIONS
Active-user migration/behavior-change guidance present where appropriate: confirmed. Evidence: `paper/main.tex:725-735` says a sandbox blocking PyPI causes C06e to degrade to `SKIP` and explains cache/mirror setup; `paper/main.tex:1105-1113` gives future-release rerun guidance; `examples/proof-chardet-relicense/REVIEW_READINESS.toml:56` says `if C06e SKIPs on this runner, document the toolchain gap and rerun on a runner where pip install from the local worktrees succeeds`.

No historical dated spec retconned without link/correction note: confirmed for the inspected dated spec/bundle claims, with caveats captured below. Evidence: `paper/main.tex:277-285` frames DAG-TOML as the current working representation and says equivalent encodings would carry the same contracts; `paper/main.tex:427-429` time-binds the LoC claim to commit `220cff4`; `paper/main.tex:1545-1549` says the manuscript was drafted from bundle commit `220cff4`. I did find stale section-number metadata in `paper/references.bib:626-627`, classified as Q6-F01.

All claimed tests actually ran with command output and status: refuted. Static validation did run and passed (`validate_numbers.py` exit 0, `all agree: True`), and JPlag persisted runInfo records `version 6.3.0`, `dateOfExecution "22/05/26"`, and `totalComparisons: 1` in `paper/figures/scripts/jplag_runInfo.json:1`. But the current live harness run did not reproduce the paper's C06e `MEASURED` row; it returned `SKIP` as documented by `paper/main.tex:725-735`. More importantly, the paper's numeric-validation text claims the C06e digest/rate are reproduced by `validate_numbers.py`, but `paper/figures/scripts/validate_numbers.py:304-307` says it does not invoke chardet for C06e, and `paper/figures/scripts/validation_report.json:143-145` records independent digest `8fbc70630c023315`, not the headline digest `58e54831f84183c7` at `paper/figures/scripts/validation_report.json:22`.

### 3. CLASSIFICATION OF THE 29 PRIOR-ART FINDINGS
S1-F01  confirmed  `paper/main.tex:1179-1180` quote: "Two of the three reviewers returned unconditional approval after one or two iteration rounds."; `paper/main.tex:1563-1566` says Codex bundle approval was Round 3.  The sentence is ambiguous across bundle vs paper review state.
S1-F02  confirmed  `paper/main.tex:86-89` quote: "a self-contained, multi-LLM-reviewed artefact"; `paper/main.tex:1601-1604` says Gemini's final-paper verdict was not part of the record.  The abstract is true for the bundle but elides the paper-review asymmetry.
S1-F03  refuted_with_evidence  `paper/main.tex:288-319` contains a five-item enumerate list.  The promised "five aspects below" are present; adding labels would be optional clarity, not a confirmed defect.
S1-F04  confirmed  `paper/main.tex:75-77` quote: "We do not adjudicate that dispute. We instead introduce a detection harness".  This is a real stylistic register choice, though not a correctness issue.

F2-F01  confirmed  `paper/main.tex:1305-1309` quote: "84 v6 ... files; the v6 file count is higher than the extractor's 87"; `paper/figures/scripts/validation_report.json:3-5` records 87 and 33 extractor files.  This is an arithmetic contradiction and the causal explanation points the wrong direction.
F2-F02  confirmed  `paper/main.tex:427-429` quote: "589 plus 228 lines of code respectively (verified via `wc -l` at commit `220cff4`)"; current `wc -l` is 589/232, while `git show 220cff4` is 589/228.  The claim is accurate only at the cited commit and fragile for readers running at HEAD.
F2-F03  confirmed  `paper/main.tex:300-303` quote: "Verdicts are drawn from a closed set: PASS / FAIL / MEASURED / OBSERVED / SKIP / DELEGATED / ABSENT / INCONCLUSIVE"; `profiles/agent-assurance/ontology.toml:210-214` defines gate verdict values only as `pass`, `fail`; `examples/proof-chardet-relicense/TRACEABILITY.toml:40` declares the 8-value bundle-local set.  The paper conflates spec vocabulary with bundle convention.
F2-F04  confirmed  `paper/main.tex:878-880` quote: "searches by what code means (structure) rather than by what code says (text)."  This overstates AST-graph structure as "meaning" in the formal semantics sense.
F2-F05  confirmed  `paper/main.tex:743-746` quote: "Both runs produced identical static signal values."; `paper/figures/scripts/validation_report.json:150-278` is a single comparison table with no sandbox-vs-workstation dimension.  The side-by-side run is not persisted in the supplied artifacts.
F2-F06  confirmed  `paper/main.tex:1122-1125` quote: "Two reviewers issued unconditional approval after iteration; one returned a small set of remaining concerns"; `paper/main.tex:1563-1595` says all three bundle reviewers issued unconditional approval.  The threats section preserves an intermediate-round framing.

L3-F01  confirmed  `paper/main.tex:934-938` quote: "sits inside the cone of plausible values"; no bounds or falsifying criterion are supplied nearby.  The corroboration claim is underdefined.
L3-F02  confirmed  `paper/main.tex:1060-1063` quote: "No known automated tool performs all of (i)--(iv) ... no longer cheap."  The cost conclusion is not established by the absence of a known tool.
L3-F03  confirmed  `paper/main.tex:1039-1042` quote: "cannot distinguish between ... copied v6's structure ... [and] converged on a similar structure"; `paper/main.tex:1050-1063` does not carry that caveat into the adversary-cost section.  The relationship is acknowledged but not reconciled where cost is argued.

U4-F01  confirmed  `paper/main.tex:1403-1406` quote: "Independent review reports are consistent that accuracy degrades"; `paper/references.bib:588-599` is a Copyleaks vendor page note, not an independent review.  The cited support does not substantiate the sentence.
U4-F02  confirmed  `paper/main.tex:1420-1425` quote: "vendor-published accuracy figures (often in the 98--99% range"; `paper/references.bib:602-614` contains no accuracy figure for CodeSpy.  The numeric range is unsourced.
U4-F03  confirmed  `paper/main.tex:1029-1030` quote: "the model's training set very likely included v6"; the cited `paper/references.bib:114-126` Carlini entry is general memorization work, and `paper/references.bib:211-218` is a news article.  The specific corpus-ingestion claim is not directly supported.
U4-F04  confirmed  `paper/main.tex:515-517` quote: "the harness's resistance criterion is graph-structural rather than tree-structural"; `paper/main.tex:405-407` lists GumTree and PDG alternatives without an empirical selection test.  The design choice is plausible but not falsified or justified by a criterion.

S2-F01  confirmed  `paper/references.bib:344-372` quotes "Internal company analysis" and "Provisional patent application"; `paper/main.tex:1201-1222` uses them to support the Verivus thesis.  The self-citation load is real and not externally verifiable from the cited entries.
S2-F02  confirmed  `paper/references.bib:588-614` quotes "Commercial SaaS product page" and "Commercial product page"; `paper/main.tex:1403-1425` makes empirical performance claims.  Vendor pages are carrying more weight than they support.
S2-F03  confirmed  `paper/main.tex:197-198` cites Daring Fireball as "General-audience framing of the clean-room question."  The reliability limitation is minor and the paper's use is appropriately scoped.
S2-F04  confirmed  `paper/references.bib:132-135` quote: "Baker v. Selden, 101 U.S. 99 (1880)"; Justia's case page identifies it as "101 U.S. 99 (1879)" at https://supreme.justia.com/cases/federal/us/101/99/.  The bibliographic year is off by the conventional citation.
S2-F05  unverifiable  `paper/references.bib:176-183` claims GitHub REST API confirmation of `gooba42`; `paper/main.tex:191-192` uses `@gooba42`.  I could not fetch the live GitHub API under the current shell restrictions and the exposed GitHub MCP did not include issue fetch.

C5-F01  confirmed  `paper/main.tex:156-158` quote: "It does not constitute legal advice"; `paper/main.tex:1000-1005` opens legal framing without repeating it.  A local disclaimer is absent from the legal section.
C5-F02  confirmed  `paper/main.tex:1022-1026` quote: "Under a strict LGPL interpretation ... structure, sequence, and organisation"; no Whelan/Altai citation appears in `paper/references.bib`.  The sentence conflates license interpretation with copyright SSO doctrine.
C5-F03  confirmed  `paper/main.tex:1457-1462` quote: "artefacts a court would actually see."  The phrase is overbroad because the harness is limited to output/public artifacts.
C5-F04  unverifiable  `paper/main.tex:57-61` lists only Werner Kasselman in the author block, and `paper/main.tex:1545-1550` discloses Claude authoring.  Whether that placement satisfies current venue policy requires external venue-policy evidence not established by the repo.

Q6-F01  confirmed  `paper/references.bib:626-627` quote: "reported in Section 9.4"; the JPlag discussion is under `paper/main.tex:1281-1282` `sec:related-tools`, not Section 9.4 in this manuscript.  The bibliography note is stale.
Q6-F02  confirmed  `paper/main.tex:673-679` quotes command/path markup including `detect.sh`, `git worktree add --detach`, and `tempdir`.  The typographic consistency issue is real but cosmetic.
Q6-F03  confirmed  `paper/main.tex:300-303` and `paper/main.tex:1092-1103` frame bundle conventions as spec properties; `profiles/agent-assurance/ontology.toml:210-214` shows the core gate verdict enum is only `pass`/`fail`.  This is the same spec-vs-bundle conflation as F2-F03.

### 4. INDEPENDENT FINDINGS
IF-CX-01  high  `paper/main.tex:992-998`  Verbatim quote: "For C06e the validation script deterministically re-derives the input corpus from the seed (`20260522`, length cap 4,096); the harness's reported `corpus_digest` of `58e54831f84183c7` is reproducible from the same seed under the harness's specific RNG-draw order, and the 0/1000 exact-match rate (the only headline number C06e reports) is reproducible across runs as well."  Problem: the validation script does not validate the C06e exact/bucket rates and does not reproduce the headline digest in its independent section. `paper/figures/scripts/validate_numbers.py:304-307` says "we do not actually invoke chardet here"; `paper/figures/scripts/validation_report.json:143-145` records independent digest `8fbc70630c023315`, while `paper/figures/scripts/validation_report.json:22` records headline digest `58e54831f84183c7`; the comparison list at `paper/figures/scripts/validation_report.json:150-276` contains no C06e exact-rate, bucket-rate, or digest comparison. Suggested fix: either make `validate_numbers.py` hash the corpus exactly as `fingerprint_behavior.py:193-194` does and execute/compare the C06e rates, or rewrite `sec:numeric-validation` to say C06e rates are taken from the persisted harness output and are not independently revalidated by `validate_numbers.py`.

IF-CX-02  medium  `paper/main.tex:1083-1088`  Verbatim quote: "Expected output is the seven-line TSV table reproduced in `tab:results` ... The exit code is 0 if no FAIL verdict landed and 1 otherwise; the gate definition treats AUX1 PASS plus C06a..C06e MEASURED as the success state."  Problem: the readiness gate does not require C06e to be `MEASURED`; `examples/proof-chardet-relicense/REVIEW_READINESS.toml:5-6` says `MEASURED / OBSERVED / DELEGATED / SKIP verdicts do NOT block the gate`, and `examples/proof-chardet-relicense/REVIEW_READINESS.toml:45-49` defines pass conditions as exit 0, every contract row, and no FAIL. My live run exited 0 with C06e `SKIP`, consistent with the gate but inconsistent with the paper's "C06a..C06e MEASURED" success wording. Suggested fix: distinguish "full-measurement run" from "readiness gate pass"; say the preferred expected output has C06e `MEASURED`, while a documented toolchain gap may produce C06e `SKIP` without a FAIL.

IF-CX-03  low  `paper/figures/scripts/run_jplag.sh:2-3`  Verbatim quote: "reported in Section 9 (Related Work, Tool Comparison subsection) of the paper."  Problem: this is a second stale section reference parallel to Q6-F01; the JPlag comparison now appears under `paper/main.tex:1281-1282` `sec:related-tools`, not Section 9. Suggested fix: replace the numeric section reference with `sec:related-tools` or remove the section number.

### 5. TERMINAL VERDICT
CONCRETE UNRESOLVABLE BLOCKERS:
1. JPlag file-count contradiction: `paper/main.tex:1305-1309` says "84 v6 ... files" and "the v6 file count is higher than the extractor's 87"; `paper/figures/scripts/validation_report.json:3-5` records 87 extractor v6 files and 33 v7 files, so 84 is lower than 87. Unblock by correcting "higher" to "lower" and revising the explanation to match the JPlag parse-error caveat at `paper/main.tex:1353-1360`, or by rerunning JPlag and replacing the counts if 84/22 are wrong.
2. C06e numeric-validation claim is false as written: `paper/main.tex:992-998` claims `validate_numbers.py` reproduces the C06e digest and 0/1000 rate, but `paper/figures/scripts/validate_numbers.py:304-307` explicitly does not invoke chardet, `paper/figures/scripts/validation_report.json:143-145` independently derives digest `8fbc70630c023315`, and the comparison table at `paper/figures/scripts/validation_report.json:150-276` contains no C06e rate/digest check. Unblock by either implementing the missing C06e validation or narrowing the paper's claim to persisted harness output only.
3. Spec-vs-bundle verdict vocabulary remains conflated: `paper/main.tex:300-303` presents the 8-value verdict set as part of the one-page DAG-TOML spec description, while `profiles/agent-assurance/ontology.toml:210-214` defines the profile gate verdict as closed `pass`/`fail` and `examples/proof-chardet-relicense/TRACEABILITY.toml:40` shows the 8-value set is bundle-local. Unblock by rewriting the spec section and later references to distinguish core/profile gate verdicts from this bundle's evidence-row verdict vocabulary.
