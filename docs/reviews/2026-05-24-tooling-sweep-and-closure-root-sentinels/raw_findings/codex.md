## Summary
U01 is **incomplete** as a standalone commit because `47b6acd` added blessed-kind DAG-TOML files without `closure_root`, and the §12 gate was red at that commit; U03 later repairs that class of defect. U02 is **incomplete** because one newly tracked TOML artifact is syntactically invalid at HEAD. U03 is **complete**: the five sentinels are root-level, canonical, self-contained, and the required §12/taplo gates pass. Terminal verdict: `concrete_unresolvable_blocker`, based on the unresolved invalid TOML in U02.

## U01 — 47b6acd
- **Finding U01-F1 — high — standalone §12 violation at commit time.** `SPEC.md:866-889` requires every blessed `[meta].template_kind` document to carry `closure_root`, including `implementation-dag` files under `tools/`. At `47b6acd`, the three added files declared blessed kinds but no root `closure_root`: `tools/claim-analysis-agent-gated-dag.toml` had `[meta]` at line 48 and `template_kind = "implementation-dag"` at line 50; `tools/review-request-dag.toml` had `[meta]` at line 25 and `template_kind = "implementation-dag"` at line 27; `tools/werner-style-policy.toml` had `[meta]` at line 43 and `template_kind = "contract-declaration"` at line 45. A detached worktree at `47b6acd` running `python3 validators/validate_closure_root.py --discover .` exited 1 with `CLOSURE-ROOT VALIDATION FAILED: 5 error(s) across 74 file(s)`, including the three U01 files.
- **Confirmed — file tracking and root absence.** At HEAD, `git ls-files` includes `tools/claim-analysis-agent-gated-dag.toml`, `tools/review-request-dag.toml`, and `tools/werner-style-policy.toml`; `claim-analysis-agent-gated-dag.toml` is absent from the repo root. At HEAD those files now carry the claimed kinds: `tools/claim-analysis-agent-gated-dag.toml:56`, `tools/review-request-dag.toml:33`, and `tools/werner-style-policy.toml:51`.
- **Classification:** `incomplete` for the `47b6acd` unit as landed, because it introduced conforming blessed-kind files without the mandatory §12 field.

## U02 — 320a901
- **Finding U02-F1 — high — newly tracked TOML is invalid.** `paper-arxiv-prep/compile-and-pdf-evidence.toml` is not parseable TOML. The first hard syntax break is at `paper-arxiv-prep/compile-and-pdf-evidence.toml:27`, where the key starts with `(a)_...`; additional invalid constructs include table-like scalar assignments at lines 44-45 and a bare Unicode arrow inside an array entry at line 52. `taplo lint paper-arxiv-prep/compile-and-pdf-evidence.toml` exited 1 with `error: invalid TOML ... line 27:1 ... unexpected token`. Independent `tomllib` parsing over U02's changed TOML set reported: `Parsed 36 TOML file(s); failures=1` and named this file. This directly refutes the commit's parse-clean claim.
- **Confirmed — `.gitignore` convention.** The new root globs match the existing `paper/.gitignore` convention: root `.gitignore` ignores `*.aux`, `*.bbl`, `*.blg`, `*.log`, `*.out`, `*.toc`, `*.fls`, `*.fdb_latexmk`, and `*.synctex.gz` at `.gitignore:51-63`, while `paper/.gitignore:1-13` uses the same LaTeX intermediate pattern and intentionally tracks PDFs/sources.
- **Confirmed — no tracked LaTeX intermediates in the new paper trees.** `git ls-files paper-arxiv-prep/ paper-hello-world/ | rg '\.(aux|bbl|blg|log|out|toc|fls|fdb_latexmk|synctex\.gz)$'` produced no matches. The tracked files are source/PDF/README/TOML/tarball artifacts only.
- **Confirmed — banned internal path prefix absent.** `git grep -n '/srv/repos/internal' 320a901 -- $(git diff --name-only 7328dfd..320a901)` produced no matches.
- **Confirmed — review-folder shape spot-check.** `docs/reviews/2026-05-23-paper-main/review_bundle.toml:5-34` declares review metadata, workflow DAG, initiator exclusion, and reviewers; `docs/reviews/2026-05-23-paper-main/gate_decision.toml:25-44` records basis and persisted review artifact links. That matches the older review-session convention visible in `docs/reviews/2026-05-23-spec-12-closure-root/`.
- **Classification:** `incomplete`, because a tracked `.toml` artifact in the U02 sweep is syntactically invalid.

## U03 — d027178
- **Confirmed — canonical root-level sentinels.** The five modified files all use `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` before the first table header: `arxiv-prep-agent-dag.toml:66-68`, `tools/claim-analysis-document-review-dag.toml:40-42`, `tools/claim-analysis-agent-gated-dag.toml:52-54`, `tools/review-request-dag.toml:29-31`, and `tools/werner-style-policy.toml:47-49`.
- **Confirmed — empty-sentinel eligibility.** §12.11 says the empty sentinel applies where there is no `[provenance]`, no `cites_upstream` mapped field, and no upstream evidence rows (`SPEC.md:1155-1171`). The five files have no top-level `provenance` or `evidence*` tables by `tomllib`, and literal search found only the self-contained comments plus non-upstream local SHA notes. The relevant kind descriptors do not map implementation-dag or contract-declaration fields to `cites_upstream` (`core/implementation-dag-kind.toml:176-181`, `core/contract-declaration-kind.toml:112-117`).
- **Confirmed — §12 gate.** `python3 validators/validate_closure_root.py --discover .` exited 0 with literal output: `CLOSURE-ROOT VALIDATION PASSED (74 file(s)).`
- **Confirmed — taplo lint.** `taplo lint arxiv-prep-agent-dag.toml tools/claim-analysis-document-review-dag.toml tools/claim-analysis-agent-gated-dag.toml tools/review-request-dag.toml tools/werner-style-policy.toml` exited 0 and reported `found files total=5 excluded=0`.
- **Classification:** `complete`.

## Process checks
- **Active migration / behaviour-change guidance:** confirmed where demanded. §12.11 gives explicit migration instructions for adding `closure_root`, choosing the empty sentinel, placing it before `[meta]`, and re-signing envelopes (`SPEC.md:1144-1191`). U02's invalid TOML remains a blocker, but not a migration-guidance issue.
- **No historical dated spec retcon:** confirmed for this range. The bundle's changed-file lists are tools/docs/paper artifacts plus five sentinel targets (`review_bundle.toml:49-53`, `75-82`, `111-117`), and `git diff --name-only 7328dfd..d027178 | rg '^(SPEC|core/|profiles/|validators/)'` produced no matches.
- **Claimed tests actually run with output/status:** partially confirmed and partially refuted. The required U03 §12 gate and five-file taplo lint were run and passed. U02's "TOMLs parse clean" claim is refuted by `paper-arxiv-prep/compile-and-pdf-evidence.toml:27` and the parse output `Parsed 36 TOML file(s); failures=1`.
- **Independent-review discipline / initiator exclusion:** confirmed. The workflow requires initiator exclusion at `tools/review-request-dag.toml:56-64`; the review bundle names `claude-opus-4-7` as initiator and excludes it from the standard reviewer set at `review_bundle.toml:125-128`. ISS-001 is open and names self-approval as the symptom at `docs/issues/2026-05-23-ISS-001-self-approval-discipline.md:2-5` and `:11-16`.

## Terminal verdict
`concrete_unresolvable_blocker` — U02 tracks an invalid TOML artifact at `paper-arxiv-prep/compile-and-pdf-evidence.toml:27`, with independent parser and taplo failures. U03's closure-root repair is sound, but the reviewed range cannot receive unconditional approval while that U02 artifact remains syntactically invalid.
