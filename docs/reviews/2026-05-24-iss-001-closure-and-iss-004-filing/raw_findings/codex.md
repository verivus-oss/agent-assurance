## Summary
U05 (`ca50b2c`) is **incomplete**. ISS-004 is correctly framed, and most of ISS-001's closing-note history checks out, but ISS-001 contains a concrete factual error: it claims the feedback memory is `memory/feedback_no_self_approval.md` and is listed in `MEMORY.md`, while neither path exists in the repo at HEAD. The closure is also premature as committed because the original acceptance criteria remain unmet as written and were not cleanly superseded in repo bytes. Terminal verdict: `concrete_unresolvable_blocker`.

## U05 — ca50b2c
- **Finding U05-F1 — high — ISS-001 closing note contains a false repo-path claim.** The closing note says `memory/feedback_no_self_approval.md` captures the discipline and is "Listed in `MEMORY.md`" at `docs/issues/2026-05-23-ISS-001-self-approval-discipline.md:221-226`. Direct existence check at HEAD returned `MEMORY.md: exists=False` and `memory/feedback_no_self_approval.md: exists=False`. The bundle instead points to a session-private file outside the repo at `/home/werner/.../memory/feedback_no_self_approval.md` (`review_bundle.toml:42-44`), and that file says the discipline applies to `SPEC.md`, `core/`, `profiles/`, `validators/`, or tracked DAG-TOML documents (`feedback_no_self_approval.md:10-13`).
- **Finding U05-F2 — high — ISS-001 is marked closed without satisfying or formally replacing its written acceptance criteria.** The issue is now `status: closed`, `closed_by: 884f290` at `docs/issues/2026-05-23-ISS-001-self-approval-discipline.md:4-9`, but the original criteria require `validators/check_review_session_terminal.py`, a repo `memory/feedback_no_self_approval.md`, and CI blocking for missing terminal verdicts at `docs/issues/2026-05-23-ISS-001-self-approval-discipline.md:169-179`. The two validator files are absent, and the repo memory file is absent.
- **Confirmed — ISS-004 itself is well framed.** It identifies the 47b6acd → d027178 sequence at `docs/issues/2026-05-24-ISS-004-blessed-kind-files-must-land-with-closure-root.md:19-30`, states the human-enforceable rule at `:66-72`, avoids mandatory new CI tooling at `:78-84` and `:95-99`, and the live CI gate invokes `validators/validate_closure_root.py --discover .` at `.github/workflows/validate.yml:178-199`.

## Q1 — ISS-001 factual accuracy
Refuted, but narrowly. The worked counter-example is correctly identified: r1 reviewed `47b6acd`, `320a901`, `d027178` (`review_bundle.toml:17-23`, `:35-123`), r2 reviewed `32936b1` (`review_bundle.toml:18-24`, `:61-94`), and r2 persisted unanimous approval (`terminal_decision.toml:25-47`, `:99-105`). The codex r1 blocker is correctly cited: `raw_findings/codex.md:9-15` identifies invalid TOML in `paper-arxiv-prep/compile-and-pdf-evidence.toml`, first hard break at line 27. Safeguards A and B are correctly absent: `validators/check_review_session_terminal.py` and `validators/check_commit_message.py` do not exist. The factual failure is the repo-memory/MEMORY claim at `docs/issues/...ISS-001...md:221-226`.

## Q2 — closing warranted
Refuted as committed. The closing note's argument that the original CI-gate framing was over-engineered is sound in principle: the session-private memory says the fix is to dispatch review, not invent infrastructure (`feedback_no_self_approval.md:27-30`, `:46-48`), and the workflow already forbids approval based on stated intent or plan-compliance claims (`tools/review-request-dag.toml:77-94`). But the issue file still carries acceptance criteria requiring repo files and CI behavior at `docs/issues/...ISS-001...md:169-179`; those criteria were not met, and the closing note adds a false repo-path claim instead of clearly recording that the criteria were superseded by user direction.

## Q3 — ISS-004 correctly framed
Verified. ISS-004 correctly identifies `47b6acd` adding three blessed-kind files without `closure_root`, `320a901` as intervening, and `d027178` adding sentinels to five files at `docs/issues/2026-05-24-ISS-004-blessed-kind-files-must-land-with-closure-root.md:19-30`. `git show d027178 --name-only` confirms the five affected files. The issue states a human-enforceable same-commit rule at `:66-72`, makes the pre-commit hook optional at `:78-84`, and explicitly says not to propose a CI gate as a substitute at `:95-99`. The existing CI mechanism is live in `.github/workflows/validate.yml:178-199`.

## Q4 — self-approval recursion
Partially each, with (b) stronger for this specific closure. Position (a) is formally correct for §12 conformance: issue files are process artefacts and out of §12.1 scope by design (`docs/issues/README.md:7-10`), and the feedback memory's hard dispatch scope names SPEC/core/profile/validator/tracked DAG-TOML changes, not issue markdown (`feedback_no_self_approval.md:10-13`). But position (b) is correct as process discipline for closing ISS-001 itself: `tools/review-request-dag.toml:56-64` excludes the initiator from the reviewer set, and `:77-94` requires approval from inspected evidence, not initiator claims. Closing the self-approval issue in a self-approved commit was exactly the boundary case that needed this independent review.

## Q5 — retcon check
Pass, with the Q1 factual error kept separate. I do not see a historical retcon of the 47b6acd / 320a901 / d027178 sequence: ISS-004's sequence matches the commit history and review findings. ISS-001 also preserves that Safeguards A and B were originally proposed but not built (`docs/issues/...ISS-001...md:228-231`). The problem is not a dated-spec retcon; it is an inaccurate current claim about repo memory paths.

## Process checks
- `confirm_active_user_migration_or_behavior_change_guidance`: partially confirmed; ISS-004 gives clear behavior guidance at `docs/issues/...ISS-004...md:66-99`, but ISS-001's guidance cites nonexistent repo memory paths.
- `confirm_no_historical_dated_spec_retconned_without_link_or_correction_note`: confirmed; `ca50b2c` changes only three `docs/issues/` files, and no `SPEC.md`, `core/`, `profiles/`, or `validators/` files.
- `confirm_claimed_tests_were_actually_run_with_command_output_and_status`: confirmed for relevant checks; `python3 validators/validate_closure_root.py --discover .` exited 0 with `CLOSURE-ROOT VALIDATION PASSED (74 file(s)).`; parsing the review bundle and workflow policy with `tomllib` exited 0.

## Terminal verdict
`concrete_unresolvable_blocker` — U05 cannot receive unconditional approval while ISS-001's closing note contains the false `MEMORY.md` / `memory/feedback_no_self_approval.md` repo-path claim and marks the issue closed without cleanly satisfying or superseding its written acceptance criteria.
