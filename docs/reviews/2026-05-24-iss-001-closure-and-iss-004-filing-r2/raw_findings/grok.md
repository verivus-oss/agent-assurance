## Summary

U06 (commit `79fe0aa`) is **complete**. The single follow-up commit closes both codex r1 high-severity blockers (U05-F1 false repo-path claims; U05-F2 unmet acceptance criteria) by (a) moving the load-bearing disciplines into a new "Review Discipline" section in the committed, contributor-visible `CONTRIBUTING.md`, (b) replacing all live references to the non-existent `memory/feedback_*.md` / `MEMORY.md` paths with explicit descriptions of per-session host-specific auto-memory plus pointers to the new `CONTRIBUTING.md` text, and (c) prefixing the original ISS-001 `## Acceptance criteria` (lines 169-179 in ca50b2c) with an unambiguous "Superseded by closing note 2026-05-24" paragraph while preserving the four items verbatim inside `~~strikethrough~~` wrappers with inline parenthetical explanations. No new defects were introduced: diff scope is exactly the three files declared in the r2 bundle, the §12 validator still passes cleanly, git history is linear with no rewrite of ca50b2c, and a full-repo search confirms zero remaining live false path claims. Re-examination of the r1 grok classification of the same MEMORY.md issue as LOW severity concludes the original rating was incorrect—the erroneous claims were written into a permanent committed ledger artefact (`docs/issues/2026-05-23-ISS-001-self-approval-discipline.md`), not transient scratch, creating a misleading process record for future readers.

## U06 — 79fe0aa

- **U06-F1 info (re-examination of r1 severity judgment)** — In the r1 review of ca50b2c, the same false repo-path claims were classified LOW (grok raw_findings:7-8, U05-F1/F2: "`Listed in `MEMORY.md`'" at ISS-001:225 and "load on entry" phrasing; also U05-F3 info on verbatim AC at :169-179). Re-examination against actual bytes: the claims were committed into `docs/issues/2026-05-23-ISS-001-self-approval-discipline.md` (a durable, git-tracked ledger file that persists in history and is treated as authoritative process record by the workflow). `ls MEMORY.md memory/feedback_no_self_approval.md` at both ca50b2c and 79fe0aa returns "No such file or directory" (verified live). Codex r1 correctly identified the exact live lines in the pre-fix version (codex raw_findings:4-6 citing :221-226, :169-179 and parallel ISS-004 lines). Because the defect lived in permanent committed bytes (not session-private memory), the r1 LOW classification under-weighted the severity; it was a process-trust error in the ledger and warranted MEDIUM or HIGH. The r2 fix (79fe0aa) correctly treats it as such by removal from live text.
- **U06-V1 verified (command output)** — `grep -n 'MEMORY.md\|memory/feedback_' docs/issues/2026-05-23-ISS-001-self-approval-discipline.md docs/issues/2026-05-24-ISS-004-blessed-kind-files-must-land-with-closure-root.md` → exactly one match: `docs/issues/2026-05-23-ISS-001-self-approval-discipline.md:189:- ~~`memory/feedback_no_self_approval.md` exists.~~ (Replaced by...` (the intentional strikethrough inside the superseded block). ISS-004 result is empty.
- **U06-V2 verified (command output)** — `ls MEMORY.md memory/feedback_no_self_approval.md memory/feedback_blessed_kind_closure_root_in_same_commit.md 2>&1` → three "No such file or directory" errors. Dead paths remain absent at HEAD.
- **U06-V3 verified (bytes)** — `CONTRIBUTING.md:49-108` (new "Review Discipline" section) contains: Section 1 "No initiator self-approval" citing `tools/review-request-dag.toml [policy.approval]` + ISS-001 + `docs/reviews/` as audit trail; Section 2 "Blessed-kind files MUST land with `closure_root` in the same commit" citing ISS-004 + SPEC §12.11 + `validate_closure_root.py` + `.github/workflows/validate.yml`; explicit "Agent-specific note on auto-memory" stating per-session facilities are "NOT part of the repo, NOT visible to other contributors... and MUST NOT be cited as load-bearing safeguards. This file (CONTRIBUTING.md) is the contributor-visible mechanism".
- **U06-V4 verified (command output)** — `python3 validators/validate_closure_root.py --discover .` → `CLOSURE-ROOT VALIDATION PASSED (74 file(s))`.
- **U06-V5 verified (command output)** — `git show --stat 79fe0aa` → exactly three files: `CONTRIBUTING.md`, `docs/issues/2026-05-23-ISS-001-self-approval-discipline.md`, `docs/issues/2026-05-24-ISS-004-blessed-kind-files-must-land-with-closure-root.md`. No spillover.
- **U06-V6 verified (bytes + history)** — Original AC text from ca50b2c (the four bullets at lines ~169-179) is preserved verbatim inside the strikethrough block at 79fe0aa:185-200 with the required "Superseded by closing note 2026-05-24" header paragraph. `git log --oneline ca50b2c~3..79fe0aa` shows linear history; ca50b2c is the direct parent of 79fe0aa; no rewrite.
- **U06-V7 verified (full-repo search)** — `git grep -n 'memory/feedback_\|MEMORY.md' -- '*.md' '*.toml'` at HEAD returns only the one expected strikethrough (line 189) plus a historical citation inside the prior review_prompt (which documents what r1 claimed, not a live assertion). Zero active false repo-path claims remain.

## R1 blocker disposition

- **U05-F1 (high — false repo-path refs in ISS-001:221-226 and ISS-004:91-119 of ca50b2c)**: **closed**. All live references to `memory/feedback_no_self_approval.md`, `memory/feedback_blessed_kind_closure_root_in_same_commit.md`, and `MEMORY.md` as repo files have been removed. The single remaining match is the intentional strikethrough at ISS-001:189 inside the superseded AC block with an explicit replacement note pointing to `CONTRIBUTING.md`. CONTRIBUTING.md:99-108 now explicitly states that per-agent auto-memory is NOT load-bearing and that the committed repo file is the mechanism. Dead paths remain absent (ls output above). The durable ledger artefact no longer contains the factual error.
- **U05-F2 (high — ISS-001 marked closed while original AC at ca50b2c:169-179 still verbatim demanded the CI infrastructure)**: **closed**. The AC section at 79fe0aa:174-200 now begins with the unambiguous paragraph "Superseded by closing note 2026-05-24. The items below were the original framing... The original items are preserved here for the audit trail, not as live requirements." All four items appear with `~~strikethrough~~` plus inline parentheticals ("Not built. Superseded by the dispatched-review discipline.", "Replaced by the `CONTRIBUTING.md` "Review Discipline" section...", etc.). Original wording is not dropped. Header status remains closed (by 884f290, the prior correct commit).

## Process checks

- `confirm_active_user_migration_or_behavior_change_guidance`: confirmed — the new "Review Discipline" section in `CONTRIBUTING.md:49-108` (with subsections 1 and 2 plus the agent-specific auto-memory note) plus the updated resolution steps and closing notes in both issue files supply explicit, contributor-visible, non-ephemeral guidance citing the exact policy locations and the worked counter-example; auto-memory is explicitly demoted to non-load-bearing convenience.
- `confirm_no_historical_dated_spec_retconned_without_link_or_correction_note`: confirmed — `git log` shows linear history with ca50b2c as direct parent of 79fe0aa; the original AC text from ca50b2c:169-179 is preserved verbatim (only wrapped); the false path claims were excised from live prose with explicit replacement notes; no SPEC.md, core/, profiles/, or validator files were altered.
- `confirm_claimed_tests_were_actually_run_with_command_output_and_status`: confirmed — the exact commands enumerated in review_prompt.md were executed in this session with recorded output: `grep -n` (one intentional strikethrough only), `ls` (three "No such file or directory"), `python3 validators/validate_closure_root.py --discover .` (exit 0, "PASSED (74 file(s))"), `git show --stat 79fe0aa` (exactly the three declared files), full-repo `git grep`, `git log`, and `git show ca50b2c:<path> | sed -n` for pre-fix AC bytes. All outputs match the required state.

## Terminal verdict

`unconditional_approval`

Both codex r1 high-severity blockers (U05-F1 and U05-F2) are closed against actual repo bytes at 79fe0aa. The fix is narrowly scoped to the three files declared in the r2 bundle, introduces no new defects (validator green, zero residual false path claims, history linear, AC text preserved for audit trail), and correctly elevates the load-bearing mechanism to a real committed file (`CONTRIBUTING.md`) while explicitly marking per-session auto-memory as non-load-bearing. The r1 grok LOW severity classification of the MEMORY.md issue is acknowledged in this review as an under-elevation given the durable committed nature of the ledger artefact; the r2 commit resolves the underlying defect. All policy requirements (`inspected_code`, `executed_tests_with_output`, `inspected_docs`, `persisted_review_evidence`) are met with file:line + command-output evidence. Per `tools/review-request-dag.toml [policy.approval]` and the r2 review_prompt.md, U06 receives unconditional approval.
