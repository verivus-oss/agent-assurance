# The discriminator is the FIELD plus the secret-specific phrasing. A bare
# "RKV02" needle would also match header-value-inlined, which is the same
# invariant reported against a different surface.

error_contains = [
  "snapshot.request.authorization",
  "inlines a secret-bearing header",
]

# The claim of this case is that the document is closure-VALID and only the
# kind layer objects. Asserting the closure layer stayed SILENT is what makes
# that claim mechanical rather than asserted.
error_not_contains = [
  "pinned closure record",
]
