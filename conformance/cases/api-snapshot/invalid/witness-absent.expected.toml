# The discriminator is the missing-TABLE diagnostic. witness-incomplete reports
# missing FIELDS inside a table that exists, so neither case's needles match the
# other, and a bare "RKV03" needle would match both.
error_contains = [
  "missing required `[snapshot.witness]` table",
  "MUST NOT be achieved by deleting the table",
]

# The claim of this case is that the document is closure-VALID and only the
# kind layer objects. Asserting the closure layer stayed SILENT is what makes
# that claim mechanical rather than asserted.
error_not_contains = [
  "pinned closure record",
]
